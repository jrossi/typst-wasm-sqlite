[package]
name = "typst_sqlite"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-minimal-protocol = "0.1"

[profile.release]
opt-level = "z"
lto = true
strip = true
